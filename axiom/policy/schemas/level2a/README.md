Level 2A schema policy artifacts for the inert Orchestrator substrate.

This directory is intentionally non-authority-bearing in this implementation
slice. Code-level schema-like validators live under `axiom/core/orchestrator/`
and are covered by deterministic unit fixtures. No manifest registration,
signing material, seal, key, live policy activation, or runtime Orchestrator
operation is created by this directory.

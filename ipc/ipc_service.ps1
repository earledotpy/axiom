# ipc/ipc_service.ps1
# Phase 2: unified IPC automation is disabled.

param(
    [switch]   $NoPosture,
    [string[]] $Agents = @("claude", "codex", "antigravity")
)

Write-Output "[ipc-neutralized] ipc_service.ps1 is disabled by Phase 2 Neutralize Raw Execution."
Write-Output "[ipc-neutralized] IPC messages are inert evidence and must be reviewed manually."
return
